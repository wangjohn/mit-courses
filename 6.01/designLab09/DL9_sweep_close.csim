#CMax simulation
import lib601.sig as sig
import math

lampAngleSignal = sig.ListSignal([-math.pi/8. + (float(i)/300)*(math.pi/4) for i in xrange(300)])
lampDistanceSignal = sig.ConstantSignal(1e-12)
nSamples = 300
deltaT = 0.02
